1. Monorepo setup (define the object definition once use in front end as well)
    - used openapi.json
    - openapi-typescript library for creating interfaces
    - apps/server contains entire FastAPI setup
    - apps/client contains entire Client setup


2. Going with int id for saving time

3. RBAC implementation
    - Authentication + RBAC (simple jwt implementation)
    - protect decorator

4. member, instructor, admin all are users with different roles

5. All db time will be in UTC 
    - it will be converted to timezone on the client side

6. postgres root user vs app user 
    - restricted access to app user (only app-db access)

7. backend has proper error handling and info logs
    - no debug logs (save time)
    - errsvc
    - logsvc

